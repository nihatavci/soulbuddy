---
phase: 01-anonymous-account-profile
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - hooks/useProfile.ts
  - app/(app)/onboarding.tsx
  - app/(app)/profile.tsx
autonomous: false
requirements: [PROF-01, PROF-02]
must_haves:
  truths:
    - "A signed-in user can set age, gender, and location during onboarding — no real name is requested"
    - "Submitting the ASL form upserts the profiles row for the current user and marks onboarded=true"
    - "After onboarding, AppGate (Plan 01-02) routes the now-onboarded user into the tabs"
    - "The profile screen reads back the user's own ASL signals"
    - "No screen collects or displays a real name; only ASL signals are shown"
  artifacts:
    - path: "hooks/useProfile.ts"
      provides: "ASL-typed profile fetch + upsert (age, gender, location, onboarded)"
      contains: "upsertProfile"
    - path: "app/(app)/onboarding.tsx"
      provides: "Anonymous ASL onboarding form wired to useUpdateProfile"
      contains: "useUpdateProfile"
    - path: "app/(app)/profile.tsx"
      provides: "ASL-only profile display + valid sign-up CTA route"
      contains: "useProfile"
  key_links:
    - from: "app/(app)/onboarding.tsx"
      to: "hooks/useProfile.upsertProfile → profiles table"
      via: "supabase.from('profiles').upsert on onConflict user_id"
      pattern: "useUpdateProfile\\(\\)"
    - from: "app/(app)/profile.tsx"
      to: "hooks/useProfile (ASL read)"
      via: "useProfile() returns age/gender/location only"
      pattern: "useProfile\\(\\)"
---

<objective>
Deliver the anonymous ASL profile slice: retype `useProfile` to ASL fields, rebuild
onboarding as a form that collects age, gender, and location (NO real name) and
upserts it to the `profiles` table from Plan 01, and render an ASL-only profile
screen. Routing into onboarding (for unonboarded users) and out to the tabs (once
`onboarded=true`) is handled by the AppGate rewritten in Plan 01-02 — this plan owns
the form, the hook, and the profile display only.

Purpose: PROF-01 (set anonymous ASL signals with no real name) and the UI side of
PROF-02 (no screen collects or shows real identity — only ASL). This is the final
vertical slice of the walking skeleton: a real UI interaction that writes to and
reads from local Postgres end-to-end.
Output: working ASL onboarding + ASL-only profile read, wired to the schema (Plan 01)
and auth/reachability (Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-anonymous-account-profile/SKELETON.md
@CLAUDE.md
@hooks/useProfile.ts
@services/supabase.ts
@app/(app)/onboarding.tsx

<interfaces>
Profile row after Plan 01 (services/database.types.ts, regenerated):
`{ id, user_id, age, gender, location, onboarded, created_at, updated_at }`
useProfile hooks (hooks/useProfile.ts) — already exist, retype to ASL:
```typescript
useProfile(): { data: Profile | null }              // fetch own profile
useUpdateProfile(): mutation(updates: Partial<...>) // upsert own profile
useCompleteOnboarding(): { completeOnboarding() }   // sets onboarded=true
```
Auth: useAuth() from context/AuthContext.tsx provides `user.id` for the upsert key.
gender allowed values (must match DB check): 'male' | 'female' | 'nonbinary' | 'other' | 'undisclosed'.
Routing: AppGate (app/(app)/index.tsx, owned by Plan 01-02) redirects authenticated
users with `onboarded !== true` to `/(app)/onboarding`, and onboarded users to the tabs.
This plan does NOT modify app/(app)/index.tsx.
Dead link to fix here: app/(app)/profile.tsx:258 pushes to `/(app)/signup` (non-existent);
retarget to `/(auth)/sign-up`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retype useProfile to ASL fields (age, gender, location)</name>
  <files>hooks/useProfile.ts</files>
  <read_first>
    - hooks/useProfile.ts (existing fetch/upsert + optimistic update logic)
    - services/supabase.ts (Profile type export off regenerated types)
    - services/database.types.ts (ASL profiles Row shape from Plan 01)
  </read_first>
  <behavior>
    - upsertProfile with { age, gender, location } writes those columns for the current user_id and returns the row.
    - useProfile returns null before any profile exists (maybeSingle), and the ASL row after upsert.
    - completeOnboarding sets onboarded=true.
  </behavior>
  <action>
    Update `hooks/useProfile.ts` so the update payload type covers `age` (number),
    `gender` (union matching the DB check constraint), and `location` (string), plus
    `onboarded`. Remove references to the old `display_name`/`avatar_url` fields. Keep the
    existing `upsertProfile` upsert on `onConflict: 'user_id'`, the optimistic cache update,
    and `useCompleteOnboarding`. Do not change query keys or MMKV persistence.
  </action>
  <verify>
    <automated>! grep -qE "display_name|avatar_url" hooks/useProfile.ts && grep -q "onboarded" hooks/useProfile.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `hooks/useProfile.ts` references `age`, `gender`, `location`, `onboarded` and no `display_name`/`avatar_url`.
    - `npm run typecheck` exits 0 (types align with regenerated `Profile`).
    - `upsertProfile` still upserts on `onConflict: 'user_id'` with optimistic update intact.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rebuild onboarding as an anonymous ASL form</name>
  <files>app/(app)/onboarding.tsx</files>
  <read_first>
    - app/(app)/onboarding.tsx (current placeholder that redirects to paywall — replace)
    - hooks/useProfile.ts (useProfile / useUpdateProfile / useCompleteOnboarding)
    - components/ui/ (existing input/button primitives to reuse — do not invent new ones)
  </read_first>
  <action>
    Replace the placeholder `app/(app)/onboarding.tsx` with an ASL form using existing
    `components/ui` primitives: a numeric `age` input (client-validate 18–120 to match the DB
    check), a `gender` selector limited to `male | female | nonbinary | other | undisclosed`,
    and a free-text `location` input (city/region label — NOT GPS/coordinates). DO NOT add any
    name/display-name/photo field. On submit, call `useUpdateProfile().mutateAsync({ age, gender,
    location, onboarded: true })`, then `router.replace('/(app)/(tabs)/index/index')`. Remove the
    paywall redirect and the Mixpanel/AppsFlyer/Facebook tracking calls (excluded SDKs) from this
    screen. Routing INTO this screen is handled by AppGate (Plan 01-02); this task only builds the
    form and writes the profile.
  </action>
  <verify>
    <automated>! grep -iqE "display_name|paywall|full_name|real name" "app/(app)/onboarding.tsx" && grep -q "useUpdateProfile" "app/(app)/onboarding.tsx" && grep -q "onboarded" "app/(app)/onboarding.tsx" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `onboarding.tsx` collects age/gender/location only; no name/photo/paywall/tracking references.
    - Submit calls `useUpdateProfile` with `onboarded: true`, then routes into the tabs.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Render ASL-only profile, fix dead link, and verify the write to local Postgres</name>
  <files>app/(app)/profile.tsx</files>
  <read_first>
    - app/(app)/profile.tsx (profile display screen + dead /(app)/signup push at line 258)
    - hooks/useProfile.ts (read hook)
    - app/(app)/onboarding.tsx (form built in Task 2)
  </read_first>
  <what-built>
    End-to-end anonymous profile slice: signed-in user fills the ASL form, it upserts to
    `profiles` in local Postgres, and the profile screen reads it back showing only ASL signals.
  </what-built>
  <action>
    Ensure `app/(app)/profile.tsx` displays only `age`, `gender`, `location` from `useProfile`
    (remove any name/display_name/avatar rendering). Change the dead `router.push('/(app)/signup')`
    (line ~258) to `router.push('/(auth)/sign-up')`. Run `supabase start` + `npm start`, sign in
    (Plan 02), complete onboarding, and confirm the write landed and no identity is shown.
  </action>
  <how-to-verify>
    1. Onboarding shows exactly three fields (age, gender, location) and no name/photo field.
    2. Submit → app enters the tabs; profile screen shows the entered ASL values only.
    3. Confirm the DB write: `supabase db execute --command "select age, gender, location, onboarded from profiles;"` shows the row with onboarded=true and no name/email column present.
    4. Confirm anonymity: `supabase db execute --command "select * from public_profiles;"` returns only user_id, age, gender, location.
    5. Confirm no dead link: `grep -c "(app)/signup" app/(app)/profile.tsx` returns 0.
  </how-to-verify>
  <acceptance_criteria>
    - A `profiles` row is written with the entered age/gender/location and `onboarded = true`.
    - `profile.tsx` renders only ASL signals — no real name, email, or photo anywhere — and no longer references `/(app)/signup`.
    - `public_profiles` exposes only user_id + ASL; no identity column present.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the ASL row is written to local Postgres and the profile screen shows ASL-only, or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input ↔ profiles table | Client-supplied ASL values cross into the DB; must not carry identity |
| user A ↔ user B | Only ASL signals may be readable by others; real identity never |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Information Disclosure | onboarding form fields | mitigate | Form collects only age/gender/location; no name/photo field exists to leak |
| T-01-10 | Information Disclosure | profile.tsx render | mitigate | Profile screen reads only ASL columns; no email/name/avatar rendered |
| T-01-11 | Tampering | ASL upsert | mitigate | Upsert keyed on current `user.id`; RLS `with check (auth.uid() = user_id)` (Plan 01) blocks writing another user's row |
| T-01-12 | Elevation of Privilege | gender/age values | mitigate | Client validates age 18–120 and gender enum; DB check constraints (Plan 01) reject out-of-range/invalid values |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; no supply-chain surface added |
</threat_model>

<verification>
- ASL onboarding writes age/gender/location to local `profiles` with onboarded=true.
- Profile screen and `public_profiles` expose only ASL signals — never a real name/email.
- profile.tsx no longer contains the dead `/(app)/signup` link.
</verification>

<success_criteria>
- PROF-01: user sets anonymous age/gender/location with no real name requested.
- PROF-02 (UI): no screen collects or displays real identity; only ASL signals appear.
</success_criteria>

<output>
Create `.planning/phases/01-anonymous-account-profile/01-03-SUMMARY.md` when done.
</output>

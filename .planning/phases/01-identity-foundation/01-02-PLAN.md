---
phase: 01-identity-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/(auth)/welcome.tsx
  - app/(auth)/age-gate.tsx
  - app/(auth)/sign-in.tsx
  - app/(auth)/sign-up.tsx
  - app/(auth)/_layout.tsx
  - app/(app)/index.tsx
  - app/(app)/account.tsx
  - app/_layout.tsx
  - context/AuthContext.tsx
  - i18n/en.json
autonomous: false
requirements: [AGE-01, AUTH-01, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "A new user sees a re:sense welcome, must confirm 18+ (no skip) before reaching sign-up, and an unconfirmed user cannot proceed"
    - "A user can create an email account and log in against the remote re:sense Supabase project"
    - "A user remains logged in after a full app restart"
    - "A user can log out from the account screen and is returned to the auth flow"
    - "An unauthenticated user landing in (app) is redirected to the auth flow — no anonymous session is auto-created"
  artifacts:
    - path: "app/(auth)/welcome.tsx"
      provides: "Welcome hero with Wordmark + entry CTA"
      min_lines: 30
    - path: "app/(auth)/age-gate.tsx"
      provides: "Blocking 18+ confirmation gate (no skip)"
      min_lines: 30
    - path: "app/(app)/index.tsx"
      provides: "AppGate: loading → auth → onboarded routing (no anon/consent/paywall)"
      contains: "Redirect"
  key_links:
    - from: "app/(auth)/age-gate.tsx"
      to: "store/mmkv Prefs"
      via: "sets age_confirmed_18_plus before allowing sign-up"
      pattern: "age_confirmed_18_plus"
    - from: "app/(app)/index.tsx"
      to: "hooks/useProfile + context/AuthContext"
      via: "onboarded + isAuthenticated gates"
      pattern: "profile\\?\\.onboarded"
    - from: "app/(auth)/sign-in.tsx"
      to: "services/supabase auth"
      via: "signInWithPassword"
      pattern: "signInWithPassword"
---

<objective>
Deliver the auth entry vertical slice: Welcome → blocking Age gate → restyled email-only sign-up/sign-in, plus the AppGate rewrite that removes the anonymous auto-session, GDPR-consent, and paywall gates. After this plan a real user can confirm 18+, create an account on the remote re:sense project, log in, stay logged in across restarts, and log out — routed toward onboarding.

Purpose: This is the second stage of the Walking Skeleton — the first real UI interaction wired to remote Supabase Auth. It closes AGE-01 and AUTH-01/02/03.
Output: Two new auth screens, restyled sign-in/sign-up, corrected routing gates, working logout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-identity-foundation/01-CONTEXT.md
@.planning/phases/01-identity-foundation/01-PATTERNS.md
@.planning/phases/01-identity-foundation/01-SKELETON.md
@PRD/design.md

<interfaces>
<!-- AuthContext public API (unchanged). From context/AuthContext.tsx:58-66, 332-338 -->
signIn(email, password): Promise<{ error: string | null }>
signUp(email, password, name): Promise<{ error: string | null }>
signOut(): Promise<void>
useAuth(): { user, session, isLoading, isAuthenticated, isAnonymous, signIn, signUp, signOut, ... }

<!-- Design tokens + Wordmark are provided by Plan 01 -->
import { AppColors, Typography, BorderRadius } from '@/constants/theme'  // re:sense palette
import { Wordmark } from '@/components/ui/Wordmark'                       // ONLY italic surface
import { Button } from '@/components/ui/Button'                          // size="lg" ≥48px CTA
import { AnimatedInputWrapper } from '@/components/ui/AnimatedPressable' // focused prop
import { Prefs } from '@/store/mmkv'                                     // Prefs.getBool/setBool
import { useT } from '@/context/LanguageContext'                        // t('...') i18n
</interfaces>

Flow order (design.md screen hierarchy 1-4): Welcome → Age gate → (sign-up/sign-in) → [onboarding, Plan 03]. Age confirmation is held pre-auth in MMKV (`age_confirmed_18_plus`) and persisted server-side as `profiles.age_confirmed_at` on the first profile write in Plan 03.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Welcome + blocking Age-gate screens, register in (auth) stack</name>
  <read_first>
    - app/(auth)/sign-in.tsx (lines 206-217 screen scaffold: View→SafeAreaView→KeyboardAvoidingView→ScrollView→card; lines 405-489 StyleSheet to mirror)
    - components/ui/Button.tsx (lines 219-226 SIZE_STYLES; size="lg" ≥48px)
    - components/shared/SplashScreen.tsx (branded splash reference)
    - constants/theme.ts (re:sense tokens from Plan 01)
    - components/ui/Wordmark.tsx (from Plan 01)
    - app/(auth)/_layout.tsx (existing Stack; add welcome + age-gate, set welcome as initial route)
    - store/mmkv.ts (Prefs.getBool/setBool API)
    - PRD/design.md (lines 53-59 accessibility/CTA rules; lines 150-164 microcopy tone + forbidden patterns)
    - .planning/phases/01-identity-foundation/01-PATTERNS.md (lines 164-171 age-gate/welcome guidance)
  </read_first>
  <action>
    Create `app/(auth)/welcome.tsx`: obsidian background, centered `Wordmark` hero, one short warm-but-not-flirty tagline (no urgency, no startup slang, no italic outside the Wordmark), and a primary `Button size="lg"` ("Continue") that routes to `/(auth)/age-gate`. A secondary text link ("I already have an account") routes to `/(auth)/sign-in`.

    Create `app/(auth)/age-gate.tsx`: full-screen BLOCKING 18+ confirmation with NO skip affordance (AGE-01). Short honest copy that this is an 18+ product. A single primary `Button size="lg"` ("I'm 18 or older") that calls `Prefs.setBool('age_confirmed_18_plus', true)` then routes to `/(auth)/sign-up`. Provide a low-emphasis "I'm under 18" / "Not yet" tertiary control that does NOT proceed — it routes back to welcome (or shows an inline, non-modal "You must be 18+ to use re:sense" message). No date-of-birth input (data minimization). Use icon + text for the gate state (yellow never carries meaning alone).

    Register both screens in `app/(auth)/_layout.tsx` and make `welcome` the initial route of the `(auth)` group. Add all copy as `t('welcome.*')` / `t('ageGate.*')` keys to `i18n/en.json` (no string concatenation).
  </action>
  <verify>
    <automated>npm run typecheck passes</automated>
    <automated>rtk grep -c "age_confirmed_18_plus" app/(auth)/age-gate.tsx returns >= 1</automated>
    <automated>rtk grep -iEc "skip" app/(auth)/age-gate.tsx returns 0 (no skip affordance)</automated>
    <automated>rtk grep -c "Wordmark" app/(auth)/welcome.tsx returns >= 1</automated>
    <automated>rtk grep -c "welcome" app/(auth)/_layout.tsx returns >= 1 AND `rtk grep -c "age-gate" app/(auth)/_layout.tsx` returns >= 1</automated>
  </verify>
  <acceptance_criteria>
    - Welcome renders the Wordmark hero and routes to age-gate; no italic outside Wordmark.
    - Age gate has a single positive-confirm CTA, no skip, no DOB field; confirming sets `age_confirmed_18_plus` and routes to sign-up; declining does not proceed.
    - Both screens registered in `(auth)/_layout.tsx` with `welcome` as initial route.
    - All strings live in `i18n/en.json`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Restyle sign-in + sign-up to re:sense, email-only</name>
  <read_first>
    - app/(auth)/sign-in.tsx (whole file — lines 31-36 GoogleSignin.configure; 79-95 password call; 223-236 mode tabs; 240-259 field pattern; 332-336 errorBox; 378-388 Google button; 405-489 StyleSheet)
    - app/(auth)/sign-up.tsx (whole file — mirrors sign-in; restyle identically)
    - components/ui/AnimatedPressable.tsx (line ~35 AnimatedInputWrapper `focused` prop)
    - components/ui/Button.tsx (variant/size props)
    - constants/theme.ts (re:sense tokens from Plan 01)
    - i18n/en.json (existing auth.signIn.* / auth.signUp.* keys)
    - .planning/phases/01-identity-foundation/01-PATTERNS.md (lines 97-128 auth restyle deltas)
  </read_first>
  <action>
    Restyle both auth screens to the re:sense dark palette by consuming `AppColors`/`Typography`/`BorderRadius` tokens — do NOT re-hardcode hex (the current card `backgroundColor: '#fff'` at sign-in.tsx:412 becomes a Coal/Soft surface token; title/labels/body use Paper/Warm-Grey tokens). Keep the existing direct-`supabase.auth.*` call logic + local error state + Haptics error feedback UNCHANGED (that is AUTH-01) and let `onAuthStateChange` drive navigation.

    Make Phase 1 EMAIL-ONLY per CONTEXT: remove/hide the password↔magic-link mode tabs (sign-in.tsx:223-236) and the OTP flow so only email + password remain; remove/hide the Google button + divider (sign-in.tsx:371-388) and the `GoogleSignin.configure` module-load block (sign-in.tsx:31-36) — if any Google code is left in, ensure it stays keyless-safe (no throw on empty `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID`). Preserve the "no account? sign up" / "have an account? sign in" cross-links.

    Apply design rules: single-column form, static labels above fields, `Button size="lg"` CTA (≥48px), body ≥16px, inline `errorBox` (never modal), restrained motion. Keep the `re:sense` Wordmark small at the top of each card. Reuse existing `t('auth.*')` keys; add any new keys to `i18n/en.json`.
  </action>
  <verify>
    <automated>npm run typecheck passes</automated>
    <automated>rtk grep -c "signInWithPassword" app/(auth)/sign-in.tsx returns >= 1 (auth logic preserved)</automated>
    <automated>rtk grep -c "GoogleSignin.signIn" app/(auth)/sign-in.tsx returns 0 (Google removed from P1 slice)</automated>
    <automated>rtk grep -ic "signInWithOtp\|verifyOtp" app/(auth)/sign-in.tsx returns 0 (OTP removed)</automated>
    <automated>rtk grep -c "backgroundColor: '#fff'" app/(auth)/sign-in.tsx returns 0 (no re-hardcoded white card)</automated>
    <automated>npm run lint passes for the two auth files</automated>
  </verify>
  <acceptance_criteria>
    - sign-in and sign-up render on the re:sense dark palette using tokens (no hardcoded `#fff` card).
    - Email + password is the only auth path; Google/OTP/magic-link removed or hidden and keyless-safe.
    - `supabase.auth.signInWithPassword` / `signUp` logic + Haptics error handling unchanged.
    - `npm run typecheck` and `npm run lint` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rewrite AppGate, fix account logout + dead route, reconcile AuthGuard</name>
  <read_first>
    - app/(app)/index.tsx (whole file — remove useEffect signInAnonymously :29-33, hasGivenConsent :36-38, Prefs onboarding_v3_complete :41-44, entitlements/paywall :48-66)
    - .planning/phases/01-identity-foundation/01-PATTERNS.md (lines 74-93 AppGate replacement snippet — the authoritative 4-step order)
    - hooks/useProfile.ts (lines 54-64 useProfile → provides profile.onboarded)
    - context/AuthContext.tsx (lines 20-33 keyless-sensitive imports; 108-168 session/onAuthStateChange — AUTH-02; 248-293 signOut + signInAnonymously)
    - app/(app)/account.tsx (lines 55-87 dead guest block pushing /(app)/signup; lines 134-153 handleSignOut pattern to KEEP — AUTH-03)
    - app/_layout.tsx (lines 62-82 AuthGuard; lines 78-80 stale anonymous-session comment)
    - components/shared/SplashScreen.tsx
  </read_first>
  <action>
    Rewrite `app/(app)/index.tsx` (AppGate) to the exact 4-step declarative order from 01-PATTERNS lines 82-91: (1) `authLoading` → `<SplashScreen/>`; (2) `!isAuthenticated` → `<Redirect href="/(auth)/welcome" />`; (3) `profileLoading` → `<SplashScreen/>`; (4) `profile?.onboarded !== true` → `<Redirect href="/(app)/onboarding" />`; else → `<Redirect href={'/(app)/(tabs)/index/index' as any} />`. Delete the anonymous auto-session `useEffect`, the `hasGivenConsent()` gate, the `onboarding_v3_complete` MMKV gate, and the entitlements/paywall gate. Use `useProfile()` for `onboarded` (NOT MMKV). Send unauthenticated users to `/(auth)/welcome` (the P1 entry) rather than an anonymous session.

    In `context/AuthContext.tsx`: leave `signInAnonymously` defined but ensure nothing in the routing path invokes it (the invocation lived only in AppGate, now removed). Verify the keyless-sensitive side effects on auth transitions (`loginRevenueCat`/`logoutRevenueCat`, `logAppsFlyerEvent`, `GoogleSignin.signOut`) all no-op with blank keys — do NOT rebuild the session/persistence logic (AUTH-02 is already wired at :108-168).

    In `app/(app)/account.tsx`: DELETE the `if (isAnonymous)` guest block (:55-87) that pushes the non-existent `/(app)/signup` route (dead-route bug). KEEP the `handleSignOut` flow (:134-153): `Alert.alert` confirm → `await signOut()` → `router.replace('/(app)/')` so AppGate re-evaluates (AUTH-03). Restyle the header/cards to re:sense tokens (drop hardcoded `#fff`).

    In `app/_layout.tsx`: update `AuthGuard` (:78-80) — replace the "AppGate will create an anonymous session" comment/assumption; unauthenticated users in `(app)` are handled by AppGate's redirect to `/(auth)/welcome`. Ensure no code relies on an auto-created anonymous session.
  </action>
  <verify>
    <automated>npm run typecheck passes</automated>
    <automated>rtk grep -c "signInAnonymously" app/(app)/index.tsx returns 0 (anon auto-session removed)</automated>
    <automated>rtk grep -c "hasGivenConsent\|useEntitlements\|paywall" app/(app)/index.tsx returns 0 (consent/paywall gates removed)</automated>
    <automated>rtk grep -c "profile?.onboarded" app/(app)/index.tsx returns >= 1 (onboarded gate from useProfile)</automated>
    <automated>rtk grep -c "/(app)/signup" app/(app)/account.tsx returns 0 (dead route removed)</automated>
    <automated>rtk grep -c "signOut" app/(app)/account.tsx returns >= 1 (logout preserved)</automated>
  </verify>
  <acceptance_criteria>
    - AppGate follows the 4-step order using `useAuth` + `useProfile`; no anon/consent/paywall gates remain; unauthenticated → `/(auth)/welcome`.
    - Nothing in the routing path invokes `signInAnonymously`; AUTH-02 session/persistence untouched.
    - account.tsx dead `/(app)/signup` push removed; `handleSignOut` preserved and functional (AUTH-03); re:sense tokens applied.
    - `app/_layout.tsx` AuthGuard no longer assumes an anonymous session.
    - `npm run typecheck` passes.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full pre-onboarding auth flow on the remote re:sense project: welcome → age gate → email sign-up/sign-in → logout → session persistence.</what-built>
  <how-to-verify>
    Run `npx expo start` and exercise on a simulator/device:
    1. Fresh launch lands on the re:sense Welcome (obsidian bg, `re:` italic + `sense` grotesk Wordmark, no italic elsewhere).
    2. Continue → Age gate. Confirm there is NO skip; tapping "I'm under 18" does NOT proceed. Tap "I'm 18 or older" → Sign-up.
    3. Sign up with a new email + password → you are logged in and routed toward onboarding (Plan 03 not built yet — landing on the onboarding placeholder/splash is expected).
    4. Fully quit and relaunch the app → you are STILL logged in (AUTH-02), not back at Welcome.
    5. Go to Account → Sign out → confirm → you return through AppGate to Welcome (AUTH-03).
    6. Sign in again with the same credentials → logged in.
    Confirm text is ≥16px, CTAs feel ≥48px, motion is restrained (140-220ms), and there are no urgency/countdown/streak patterns.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (e.g., italic leak, skip present, not staying logged in).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → age gate | Self-declared 18+; the store-minimum adult gate before any content |
| RN client → Supabase Auth | Untrusted credentials cross here; Supabase handles hashing/session |
| (auth) group ↔ (app) group | expo-router boundary; gates must prevent unauthenticated entry to (app) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Elevation of Privilege | age gate | mitigate | Blocking gate, no skip; `age_confirmed_18_plus` required before sign-up route; persisted server-side as `age_confirmed_at` in Plan 03 |
| T-01-07 | Tampering | deep-link into (app) | mitigate | AppGate redirects `!isAuthenticated` → `/(auth)/welcome`; `onboarded` gate; AuthGuard no longer auto-creates a session |
| T-01-08 | Spoofing | email auth | mitigate | Supabase Auth password hashing; email trimmed+lowercased; rely on Supabase password policy |
| T-01-09 | Information Disclosure | auth error copy | accept | Generic "account already exists" message on sign-up; acceptable for closed beta |
| T-01-10 | Information Disclosure | analytics on auth events | mitigate | Excluded SDKs (AppsFlyer/Mixpanel) no-op on blank keys (Plan 01 keyless boot); no PII emitted in beta |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages added in this plan |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` pass.
- Grep gates confirm anon/consent/paywall removal, dead-route removal, email-only auth, age-gate has no skip.
- Human-verify checkpoint confirms the end-to-end flow against remote Supabase incl. restart persistence and logout.
</verification>

<success_criteria>
- AGE-01: unconfirmed/under-18 users cannot reach sign-up or app content.
- AUTH-01/02/03: email sign-up + login work against remote Supabase, session survives restart, logout returns to the auth flow.
- No anonymous auto-session; routing is deterministic through welcome → age gate → auth → onboarding.
</success_criteria>

<output>
Create `.planning/phases/01-identity-foundation/01-02-SUMMARY.md` when done.
</output>

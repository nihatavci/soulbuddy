/**
 * Empty mock for expo/src/winter/ImportMetaRegistry
 * Prevents jest-runtime "outside scope" error when expo's lazy getter fires
 * during module initialization in the test environment.
 */
module.exports = { ImportMetaRegistry: {} };

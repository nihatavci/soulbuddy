{
  "name": "fliq-auth-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Clerk Auth Cloudflare Worker — handles auth for Fliq mobile app",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4",
    "react-dom": "^19.2.4",
    "react-native-svg": "15.12.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250224.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.0",
    "wrangler": "^4.3.0"
  }
}
